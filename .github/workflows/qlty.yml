on:
  push:
    branches:
      - master-yukihiko-shinoda
jobs:
  call-workflow:
    permissions:
      contents: read
      id-token: write
    uses: yukihiko-shinoda/invoke-lint/.github/workflows/reusable-qlty.yml@main
